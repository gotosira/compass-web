app_identifier("com.sira.Compass") # The bundle identifier of your app
apple_id(ENV["APPLE_ID"] || "")      # Your Apple ID (email)

itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"] || "")
team_id(ENV["DEVELOPER_PORTAL_TEAM_ID"] || "")

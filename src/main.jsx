import React from "react";
import { createRoot } from "react-dom/client";
import App from "../circle_divided_into_64_parts_canvas.jsx";

const container = document.getElementById("root");
const root = createRoot(container);
root.render(<App />);


